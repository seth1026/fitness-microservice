spring:
  config:
    import: ""
  application:
    name: activity-service-test
  rabbitmq:
    host: localhost
    port: 5672

rabbitmq:
  exchange:
    name: fitness-exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking

eureka:
  client:
    enabled: false
